JWT_SECRET=
JWT_REFRESH_SECRET=
PORT=3001
DB_PATH=./data/fantasy.db
# Comma-separated list of origins allowed through CORS in production.
# Must not be empty when NODE_ENV=production.
ALLOWED_ORIGINS=http://localhost:5173
NODE_ENV=development
